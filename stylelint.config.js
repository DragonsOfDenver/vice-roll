// stylelint.config.js
module.exports = {
    extends: 'stylelint-config-standard-scss', // Use a SCSS-specific configuration
    plugins: [
      'stylelint-scss', // Load the stylelint-scss plugin
    ],
    rules: {
      // Additional rules can be added here
    },
  };
  